{
  "name": "task-prompt",
  "version": "1.0.0",
  "description": "Task Prompt Application",
  "scripts": {
    "start": "concurrently \"pnpm --filter task-prompt-server dev\" \"pnpm --filter task-prompt-client dev\"",
    "server": "pnpm --filter task-prompt-server dev",
    "client": "pnpm --filter task-prompt-client dev",
    "install-all": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
